```mermaid
graph TD
    A[开始] --> B{初始化：\n- 开放列表 (Open List) = {起始节点} \n- 关闭列表 (Closed List) = {}}
    B --> C{从开放列表中选择代价最小的节点 N}
    C --> D{N 是否为目标节点？}
    D -- 是 --> E[结束：找到目标节点]
    D -- 否 --> F{将 N 从开放列表移到关闭列表}
    F --> G{扩展节点 N，获得所有子节点}
    G --> H{检查每个子节点 S}
    H --> I{S 是否在开放列表或关闭列表中？}
    I -- 是 --> J{计算从起始节点经过 N 到 S 的新代价}
    J --> K{新代价是否小于 S 的当前代价？}
    K -- 是 --> L{更新 S 的代价和父节点}
    K -- 否 --> H
    L --> H
    I -- 否 --> M{添加到开放列表中，并设置代价和父节点}
    M --> H
    H --> N{是否所有子节点都已检查？}
    N -- 否 --> H
    N -- 是 --> O{开放列表是否为空？}
    O -- 是 --> P[结束：搜索失败，未找到目标节点]
    O -- 否 --> C

```